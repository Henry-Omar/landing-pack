# Landing Pack backend is pure Python standard library — NO third-party deps.
# Runtime: Python 3.8+ (tested on 3.11).
# If you add packages later, list them here and run: pip install -r requirements.txt
